name: hello
description: Simple greeting skill
version: 1.0.0

inputs:
  - name: name
    type: string
    description: Name to greet
    required: true
    default: World

output:
  type: string
  description: Greeting message

execution:
  command: python
  script: run.py
  timeout: 30
